# Superseded: layer deterministic policy before isolated automated review

> Superseded by the version 2 unified Rule model.

This record described version 1's separate Approval Rules and ordered Policy Rules. Version 2 replaces both with one Rule type and fixed restrictive conflict semantics. The durable part remains: deterministic user Rules and minimal defaults run before a fresh tool-less Reviewer.
